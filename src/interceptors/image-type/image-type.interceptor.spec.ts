import { ImageTypeInterceptor } from './image-type.interceptor';

describe('ImageTypeInterceptor', () => {
  it('should be defined', () => {
    expect(new ImageTypeInterceptor()).toBeDefined();
  });
});
